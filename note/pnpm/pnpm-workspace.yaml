packages:
  # all packages in direct subdirs of packages/ 所有前端项目存储的目录
  - 'packages/*'
  # all packages in subdirs of components/ 公共组件 
  - 'components/**'
  # 公共的方法 api
  - 'api/**'
  # exclude packages that are inside test directories 过渡
  - '!**/test/**'